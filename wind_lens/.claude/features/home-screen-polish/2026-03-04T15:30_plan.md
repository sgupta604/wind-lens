# Plan: home-screen-polish

| Field | Value |
|-------|-------|
| Feature | home-screen-polish |
| Created | 2026-03-04T15:30 |
| Status | complete |
| Based on | User-provided requirements (inline research) |
| Estimated effort | ~10 minutes |

## Summary

Three cosmetic fixes to the home screen: correct the wind speed unit label from "mph" to "m/s", brighten dim header/sublabel colors in the wind data row, and brighten the subtitle color in the top bar.

## Architecture

No architectural changes. This is a text/color constant update across two widget files.

## Files to Modify

### 1. `lib/features/home/widgets/home_wind_row.dart`

| Line | Current | New | Reason |
|------|---------|-----|--------|
| 55 | `'Wind speed $speedValue miles per hour'` | `'Wind speed $speedValue meters per second'` | Semantics label matches actual unit |
| 60 | `'mph'` | `'m/s'` | Wind API returns m/s, not mph |
| 129 | `Color(0xFF333333)` | `Color(0xFF555555)` | Header labels too dim on dark background |
| 147 | `Color(0xFF444444)` | `Color(0xFF777777)` | Sublabels too dim on dark background |

### 2. `lib/features/home/widgets/home_top_bar.dart`

| Line | Current | New | Reason |
|------|---------|-----|--------|
| 78 | `Color(0xFF444444)` | `Color(0xFF666666)` | "ATMOSPHERIC AR" subtitle too dim |

## Files to Add/Modify for Tests

### 3. `test/features/home/widgets/home_wind_row_test.dart`

Add one new test that verifies the speed column displays the unit label "m/s".

## Testing Strategy

| Type | Count | Description |
|------|-------|-------------|
| Unit (new) | 1 | Verify speed unit label is "m/s" |
| Unit (existing) | 6 | Existing HomeWindRow tests still pass |
| Regression | All | Full `flutter test` suite passes |

## Non-Goals

- Not changing font sizes or weights
- Not changing the altitude "ft AGL" unit (that is correct)
- Not adding dark mode variants
- Not refactoring _DataColumn to accept Color parameters
